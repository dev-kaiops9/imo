/**
 * pdf.js
 * -----------------------------------------------------------------------
 * PLACEHOLDER SEMENTARA — file asli tidak berhasil dipulihkan dari backup.
 * jsPDF sudah dimuat di index.html; logika penyusunan PDF final (identik
 * dengan tampilan preview.js) perlu ditulis ulang di sini saat integrasi
 * backend dilanjutkan.
 * -----------------------------------------------------------------------
 */

const Pdf = {
  async build(data) {
    // TODO: susun dokumen jsPDF sesuai CONFIG.getTableColumns, lalu
    // kembalikan Blob untuk diunggah lewat Api.saveSerahTerima().
    console.log("[stub] Pdf.build called with:", data);
    return null;
  },
};
